{
  "name": "coding-agent-hub",
  "version": "1.0.0",
  "description": "Browse and review coding agent sessions",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.renderer.json",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "electron": "^34.0.0",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "marked": "^17.0.5"
  }
}
